import React from 'react';
import LoginForm from '../components/LoginForm/LoginForm';

const LoginPage = () => {
  const i = 0;
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
